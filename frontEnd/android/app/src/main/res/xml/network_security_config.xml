<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Configuration for Creature Archive.
  Allows cleartext HTTP to local dev servers during development.
-->
<network-security-config>
    <!-- Allow cleartext for local dev servers -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>

    <!-- Default: cleartext permitted (overridden per-build by manifest flag) -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
